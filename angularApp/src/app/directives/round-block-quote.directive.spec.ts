import { RoundBlockQuoteDirective } from './round-block-quote.directive';

describe('RoundBlockQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBlockQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
