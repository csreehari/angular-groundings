import { ItalicCasePipe } from './italic-case.pipe';

describe('ItalicCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ItalicCasePipe();
    expect(pipe).toBeTruthy();
  });
});
